[package]
name = "infrastructure"
version = "0.1.0"
edition = "2024"

[dependencies]
application = {path = "../application"}
domain = {path = "../domain"}
shared = {path = "../shared"}

[dev-dependencies]
chrono = "0.4"
